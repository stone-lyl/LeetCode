/**
 * @param {string} s
 * @param {string} p
 * @return {boolean}
 */
var isMatch = function(s, p) {
    for(let i = 0; i<s.length; i++){
        s[i]
    }
    console.log(s === p);
};
isMatch("aa", "aa");